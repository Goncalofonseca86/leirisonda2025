// SyncNotifications component disabled to prevent duplicate notifications
// All sync functionality remains active in background via useFirebaseSync hook
// Status indicator available in SyncNotification component

export function SyncNotifications() {
  return null;
}
